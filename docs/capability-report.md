# model-atlas capability report

Generated by `model-atlas preflight`. Machine-readable snapshot of the runtime
for the real GLM-5.2 two-DGX-Spark experiment. Every fact measured here, not
guessed.

- **model_atlas_version**: 1.0.0
- **python / arch**: 3.12.3 / aarch64 (DGX Spark, ARM64)
- **GPU**: 1 x NVIDIA GB10 (compute capability present); 121 GiB host RAM
- **Active GPU services** (production workloads — do NOT stop): a vLLM worker
  (`VLLM::Worker_TP0`, ~10.2 GiB) and a `llama-server` (~3.7 GiB). These consume
  exclusive ULM capacity; full-model experiment runs must respect them.
- **Execution backends (torch/transformers/vllm/sglang/modelopt/exllamav2/safetensors)**:
  all ABSENT in this venv. No real tensor-body forward/trace or distributed
  runtime dependency exists yet — the repo has a synthetic plane + metadata
  census only. This is an active blocker for Phases 2/5/6 execution work (report,
  do not fabricate).

Mounted source model:
- `GLM-5.2-NVFP4` at `/media/glm52/models/nvidia/GLM-5.2-NVFP4` (~503 GiB,
  47 shards). Immutable parent; never rewritten in place.

Commands:
```bash
model-atlas preflight --out capability_report.json
```
